<!--
Title must be a Conventional Commit, at most 72 characters, no trailing period:

  feat(webui): add a show-hidden toggle in the folder picker
  fix(git-import): run git clone without the bundled libssl
  feat(session-import)!: default the importer source to auto

See CONTRIBUTING.md and .github/COMMIT_CONVENTION.md.
-->

## Why

## How I verified

<!--
Breaking PRs only (`type!:` in the title). Delete this section otherwise.
Add a footer line exactly like:

BREAKING CHANGE: callers must pass the source explicitly.

Apply the `semver:major` label. CI rejects a bang without both the footer
and the label.
-->
