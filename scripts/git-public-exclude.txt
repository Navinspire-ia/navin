# Chemins jamais poussés vers GitHub (https://github.com/Navinspire-ia/navin).
# Une ligne = préfixe. `site/` exclut tout le dossier. Commentaires : # ...
#
# Arbre public = CLI + WebUI + récupération publique des templates.
# Hors arbre : site vitrine, desktop packagé (Tauri / Electron), os/,
# AWS publish, navin.live / provider Navin.

# Site vitrine + API licence / Stripe / admin / Supabase
site/
src/

# Desktop packagé (Tauri, Electron) et binaires. La WebUI source reste publique.
desktop/
desktop-electron/
os/

# Images Docker du site (front :3100, back :3101) et leur .dockerignore :
# tout deploy/ ne concerne que le site.
deploy/

# Sous-module navin-engine (daemon Evolve, optionnel) : hébergé sur l'ancien
# compte suspendu, le lien public serait cassé. À réactiver quand le dépôt
# aura une nouvelle adresse (retirer ces deux lignes + mettre à jour l'URL).
.gitmodules
crates/

# Publication S3 / politique du bucket (binaires). La lecture publique
# des templates reste dans navin/templates/apps/s3pack.py.
packaging/aws/
packaging/linux/
packaging/macos/
packaging/windows/
scripts/publish-os-to-s3.sh
scripts/publish-templates-to-s3.sh
scripts/publish-media-templates-to-s3.sh
scripts/s3-bucket-policy.json

# Masters locaux : le CLI les télécharge depuis l'URL publique
templates/

# Connexions navin.live + provider managé
navin/license_client.py
navin/license_sync.py
navin/webui/account_api.py

# Tests qui importent ces modules au niveau module : sans eux la collecte
# pytest de l'arbre public s'interrompt (ImportError).
tests/test_license_client.py
tests/test_license_sync.py
tests/test_live_secrets_roundtrip.py
tests/test_webui_account_api.py

# Interne, logs, exports locaux, fixtures de preview
.navin/
marketing/
artifacts/
tmp-preview-fixture/
notarization-log.json

# Audits internes et preuves (captures Tauri, rapports, docx/pptx)
audit/
docs/audits/
