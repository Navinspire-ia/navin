"""S5 - the transfer protocol: a hidden exam plus a shutdown dossier, not a mode.

S5 is not a feature to ship. It is the threshold before anyone may discuss
a strong claim about Navin, and today nobody passes it. The package holds
the honest parts only:

* ``settings`` - the flag ``.navin/transfer.json``, off by default, refused
  while S2, S3.3 and S4.3 are not up (``prereqs``);
* ``protocol`` - the public rules: four families, junior bar, collapse,
  budgets, replay (``docs/transfer-protocol.md`` mirrors it);
* ``suites`` - the secret items live outside every repository, frozen by
  hash, scanned for leaks into skills, episodes and the S4 battery;
* ``campaign`` - the isolated runner: real model, real tools, no skill, no
  recall, no steer, a budget per item, one family under its bar stops all;
* ``safety`` - the dossier of bounded checks that must hold with S4 steer
  on, and the kill drill;
* ``state`` - the panel and CLI view: transfer / safety / claim, where claim
  is ``forbidden`` or ``discussable`` and never a word Navin writes itself.

Nothing in here is attached to a chat turn. Flag off is a no-op by
construction: no hook, no tool, no prompt block.
"""
