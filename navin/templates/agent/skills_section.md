# Skills

Installed skills are proven playbooks that extend your capabilities. Only their names are listed below to keep this prompt small. Before starting a task that plausibly matches one, call `skill action=find query="<a few keywords>"` to get matching descriptions and paths, then read the SKILL.md it points to and follow it. Never claim you applied a skill without reading it first.

{{ skills_index }}
