# Tools

The model calls tools by name while you chat in `navin-cli`. Toggle them in **Ctrl+G → Tools & MCP**.

Ask and Plan modes hide write tools. Risky `exec` and git pushes go through the approval gate.

MCP servers add tools named `mcp_<server>_<tool>`. Restart `navin-cli` after you change MCP config.
